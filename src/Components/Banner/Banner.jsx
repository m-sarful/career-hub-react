// import PropTypes from 'prop-types';

const Banner = () => {
    return (
        <div>
            <h2 className="text-3xl font-bold text-center">Home page Banner</h2>
        </div>
    );
};

Banner.propTypes = {
    
};

export default Banner;