// import PropTypes from 'prop-types';

const Banner = () => {
    return (
        <div>
            <h2>Banner text here</h2>
        </div>
    );
};

Banner.propTypes = {
    
};

export default Banner;